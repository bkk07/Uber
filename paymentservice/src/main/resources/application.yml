spring:
  application:
    name: payment-service

  datasource:
    url: jdbc:mysql://localhost:3306/uber_payments?useSSL=false&serverTimezone=UTC
    username: root
    password: Kiran@210800
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

razorpay:
  api:
    key: rzp_test_RFzM8sxvEqIb5Z
    secret: UMrCRDL7yCeKgcjoS6V0N1UX
server:
  port: 8089
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace